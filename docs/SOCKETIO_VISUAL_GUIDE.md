# Socket.IO Connection Flow - Visual Guide

```
╔═══════════════════════════════════════════════════════════════════════════╗
║                    SOCKET.IO REAL-TIME COMMUNICATION                       ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│                           INITIAL CONNECTION                                 │
└─────────────────────────────────────────────────────────────────────────────┘

   Frontend (React/Vue/JS)                    Backend (Node.js + Socket.IO)
   ━━━━━━━━━━━━━━━━━━━━━━                    ━━━━━━━━━━━━━━━━━━━━━━━━━━━

   1. User logs in
      POST /api/auth/login
      { email, password }
                │
                ▼
      Receives JWT token ──────────────────────▶ Validates credentials
                │                                Returns token
                │
   2. Connect Socket.IO with token
      socketService.connect(token)
                │
                │  WebSocket Handshake
                │  ┌──────────────────────┐
                │  │ auth: { token: ... } │
                └──┤ transports: ['ws']   │
                   └──────────────────────┘
                            │
                            ▼
                   ┌────────────────────────────┐
                   │  Socket.IO Middleware      │
                   │  ▸ Verify JWT token        │
                   │  ▸ Decode user ID          │
                   │  ▸ Fetch user from DB      │
                   │  ▸ Attach to socket        │
                   └────────────────────────────┘
                            │
                            ▼
      ◀──────────────── CONNECTION SUCCESS
      socket.id assigned
      userId attached
                            │
                            ▼
                   ┌────────────────────────────┐
                   │  Auto-Join Rooms           │
                   │  ▸ user_{userId}          │
                   │  ▸ admin (if admin/staff)  │
                   │  ▸ Role-based rooms        │
                   └────────────────────────────┘
                            │
                            ▼
   ✅ CONNECTED & READY TO RECEIVE EVENTS


┌─────────────────────────────────────────────────────────────────────────────┐
│                      EVENT FLOW - LOGIN NOTIFICATION                         │
└─────────────────────────────────────────────────────────────────────────────┘

   User A (Mobile)                Backend                    User A (Desktop)
   ━━━━━━━━━━━━━━━               ━━━━━━━━                   ━━━━━━━━━━━━━━━━━
   
   1. Login from mobile
      POST /api/auth/login
                │
                ▼
           ┌─────────────────┐
           │ Authenticate    │
           │ Extract IP      │
           │ Get location    │
           │ Parse device    │
           └─────────────────┘
                │
                ▼
           ┌─────────────────────────────────┐
           │ Send Notifications              │
           │ ├─ Email ✅                     │
           │ ├─ Web Push ✅                  │
           │ └─ Socket.IO ✅                 │
           └─────────────────────────────────┘
                │
                ▼
           io.to('user_12345').emit()
                │
                ├──────────────────────────────▶ 2. Desktop receives
                │                                  login_notification event
                │                                  │
                │                                  ▼
                │                                  Show notification:
                │                                  "New login from Mobile"
                │                                  "Location: New York"
                │
                └────▶ Mobile gets response
                       Login successful!


┌─────────────────────────────────────────────────────────────────────────────┐
│                       ROOM ARCHITECTURE                                      │
└─────────────────────────────────────────────────────────────────────────────┘

                          ┌─────────────────┐
                          │  Socket.IO      │
                          │  Server         │
                          └────────┬────────┘
                                   │
                ┏━━━━━━━━━━━━━━━━━━┻━━━━━━━━━━━━━━━━━━┓
                ┃                                       ┃
                ┃            ROOM MANAGEMENT            ┃
                ┃                                       ┃
                ┗━━━━━━━━━━━━━━━━━━┳━━━━━━━━━━━━━━━━━━┛
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │
        ▼                          ▼                          ▼
   ┌─────────┐              ┌─────────┐               ┌─────────┐
   │ user_1  │              │ user_2  │               │  admin  │
   │         │              │         │               │         │
   │ socket1 │              │ socket3 │               │ socket2 │
   │ socket2 │              │ socket4 │               │ socket5 │
   └─────────┘              └─────────┘               └─────────┘
       │                          │                          │
       │                          │                          │
   Personal                   Personal                  Admin-only
   notifications              notifications             alerts


┌─────────────────────────────────────────────────────────────────────────────┐
│                    EVENT TYPES & DATA FLOW                                   │
└─────────────────────────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  CLIENT → SERVER         ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━┛

socketService.emit('event', data)
              │
              ▼
    ┌────────────────────┐
    │ join_room          │──▶ Join specific room
    ├────────────────────┤
    │ leave_room         │──▶ Leave specific room
    ├────────────────────┤
    │ mark_notification  │──▶ Mark as read
    │ _read              │
    ├────────────────────┤
    │ typing             │──▶ User is typing
    ├────────────────────┤
    │ stop_typing        │──▶ User stopped typing
    └────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  SERVER → CLIENT         ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━┛

io.to(room).emit('event', data)
              │
              ▼
    ┌────────────────────────┐
    │ login_notification     │──▶ New login detected
    ├────────────────────────┤
    │ notification           │──▶ General notification
    ├────────────────────────┤
    │ order_update           │──▶ Order status changed
    ├────────────────────────┤
    │ cart_update            │──▶ Cart synchronized
    ├────────────────────────┤
    │ flash_sale_start       │──▶ Flash sale started
    ├────────────────────────┤
    │ flash_sale_end         │──▶ Flash sale ended
    ├────────────────────────┤
    │ inventory_update       │──▶ Stock changed
    ├────────────────────────┤
    │ admin_alert            │──▶ Admin notification
    └────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                    COMPLETE DATA FLOW EXAMPLE                                │
└─────────────────────────────────────────────────────────────────────────────┘

Frontend                      Backend                      Database
━━━━━━━━                      ━━━━━━━━                     ━━━━━━━━━

1. User adds item to cart
   ├─ POST /api/cart
   │  { productId, quantity }
   │
   └──▶ Receives response
        ▲
        │
2. Backend processes                     3. Save to MongoDB
   ├─ Validate product                      ├─ Update cart
   ├─ Check inventory      ───────────────▶ ├─ Update stock
   ├─ Update cart                           └─ Save changes
   │
4. Emit Socket event
   ├─ io.to('user_123').emit('cart_update', {
   │    action: 'add',
   │    cart: updatedCart
   │  })
   │
5. ALL connected devices receive ◀────────┘
   ├─ Desktop: Update cart UI
   ├─ Mobile: Show notification
   └─ Tablet: Sync cart display


┌─────────────────────────────────────────────────────────────────────────────┐
│                    FRONTEND IMPLEMENTATION FLOW                              │
└─────────────────────────────────────────────────────────────────────────────┘

App Component
   │
   ├─▶ 1. User logs in
   │      └─ Get JWT token
   │
   ├─▶ 2. Initialize Socket.IO
   │      └─ socketService.connect(token)
   │
   ├─▶ 3. Setup Event Listeners
   │      ├─ socketService.on('login_notification', handler)
   │      ├─ socketService.on('order_update', handler)
   │      ├─ socketService.on('cart_update', handler)
   │      └─ socketService.on('notification', handler)
   │
   ├─▶ 4. Join Specific Rooms (optional)
   │      ├─ socketService.joinRoom('order_12345')
   │      └─ socketService.joinRoom('cart_' + userId)
   │
   ├─▶ 5. Handle Events
   │      └─ Update UI when events received
   │
   └─▶ 6. Cleanup on Unmount/Logout
          ├─ Remove event listeners
          ├─ Leave rooms
          └─ Disconnect socket


┌─────────────────────────────────────────────────────────────────────────────┐
│                    RECONNECTION FLOW                                         │
└─────────────────────────────────────────────────────────────────────────────┘

Frontend                          Backend
━━━━━━━━                          ━━━━━━━━

Connected ✅
   │
   │  Network drops
   │  Connection lost ❌
   │
   ▼
Disconnect detected
   │
   ├─ Show "Connecting..." indicator
   │
   ▼
Auto-reconnect attempt 1
   │  Wait 1 second
   │
   ├─────────────────────▶ Connection refused ❌
   │
   ▼
Auto-reconnect attempt 2
   │  Wait 2 seconds
   │
   ├─────────────────────▶ Connection refused ❌
   │
   ▼
Auto-reconnect attempt 3
   │  Wait 4 seconds
   │
   ├─────────────────────▶ Connection SUCCESS ✅
   │
   │◀────────────────────
   │  'connect' event
   │
   ▼
Reconnected ✅
   │
   ├─ Hide "Connecting..." indicator
   ├─ Rejoin rooms
   ├─ Sync data
   └─ Show "Back online" notification


┌─────────────────────────────────────────────────────────────────────────────┐
│                    SECURITY ARCHITECTURE                                     │
└─────────────────────────────────────────────────────────────────────────────┘

                    ┌──────────────────────────┐
                    │  Socket.IO Connection    │
                    │  Request with Token      │
                    └────────────┬─────────────┘
                                 │
                                 ▼
                    ┌──────────────────────────┐
                    │  Authentication          │
                    │  Middleware              │
                    │  ─────────────────       │
                    │  1. Extract token        │
                    │  2. Verify JWT           │
                    │  3. Check expiration     │
                    │  4. Fetch user           │
                    └────────────┬─────────────┘
                                 │
                    ┌────────────┴─────────────┐
                    │                          │
                    ▼                          ▼
            ✅ Valid Token              ❌ Invalid Token
                    │                          │
                    │                          ▼
                    │                  Connection rejected
                    │                  'connect_error' event
                    │
                    ▼
        ┌───────────────────────┐
        │  Attach User Context  │
        │  socket.userId        │
        │  socket.userRole      │
        │  socket.user          │
        └───────────┬───────────┘
                    │
                    ▼
        ┌───────────────────────┐
        │  Room Assignment      │
        │  ─────────────────    │
        │  • user_{userId}      │
        │  • admin (if admin)   │
        │  • Role-based rooms   │
        └───────────┬───────────┘
                    │
                    ▼
            ✅ Ready to receive
               secure events


┌─────────────────────────────────────────────────────────────────────────────┐
│                    PERFORMANCE OPTIMIZATION                                  │
└─────────────────────────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  BEST PRACTICES        ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━┛

1. Lazy Connection
   ├─ Only connect when user is authenticated
   └─ Don't connect on every page load

2. Event Cleanup
   ├─ Always remove listeners when component unmounts
   └─ Prevents memory leaks

3. Room Management
   ├─ Join rooms only when needed
   └─ Leave rooms when done

4. Debounce Frequent Events
   ├─ Cart updates
   ├─ Typing indicators
   └─ Real-time search

5. Connection Status
   ├─ Show indicator to user
   └─ Handle reconnection gracefully

6. Error Handling
   ├─ Catch connection errors
   ├─ Handle timeouts
   └─ Provide fallbacks


┌─────────────────────────────────────────────────────────────────────────────┐
│                    QUICK REFERENCE                                           │
└─────────────────────────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  CONNECTION                                                                 ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

socketService.connect(token, 'http://localhost:5000')
socketService.disconnect()
socketService.isConnected()

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  EVENTS                                                                     ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

// Listen
socketService.on('event_name', (data) => { ... })

// Emit
socketService.emit('event_name', { data })

// Remove
socketService.off('event_name', handler)

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  ROOMS                                                                      ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

socketService.joinRoom('room_name')
socketService.leaveRoom('room_name')

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  CONNECTION STATUS                                                          ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

socketService.on('connect', () => console.log('Connected'))
socketService.on('disconnect', () => console.log('Disconnected'))
socketService.on('connect_error', (err) => console.error(err))


═══════════════════════════════════════════════════════════════════════════════

                          🎉 YOU'RE READY TO GO! 🎉

═══════════════════════════════════════════════════════════════════════════════

Next Steps:
  1. Install socket.io-client
  2. Create socket service
  3. Connect with JWT token
  4. Setup event listeners
  5. Test with login notifications

Full Guide: docs/SOCKETIO_FRONTEND_GUIDE.md

═══════════════════════════════════════════════════════════════════════════════
```
